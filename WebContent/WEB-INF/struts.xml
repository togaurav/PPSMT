<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.1//EN"
    "http://struts.apache.org/dtds/struts-2.1.dtd">
<struts>
     <constant name="struts.custom.i18n.resources" value="globalMessages" />
     <!-- 默认的视图主题 -->  
     <constant name="struts.ui.theme" value="simple" />
     <constant name="struts.i18n.encoding" value="UTF-8" />
     <!-- 指定被struts2处理的请求后缀类型    -->  
     <constant name="struts.action.extension" value="action"/>  
     
     <constant name="struts.devMode" value="true" />
     <!--  配置Convention插件自动重加载映射  -->
	 <constant name="struts.convention.classes.reload" value="true" /> 
     <!-- 当struts.xml改动后，是否重新加载。默认值为false(生产环境下使用)  -->  
     <constant name="struts.configuration.xml.reload" value="true"/>
      <!-- 设置浏览器是否缓存静态内容。默认值为true(生产环境下使用)  -->  
     <constant name="struts.serve.static.browserCache" value="false" /> 
     <!-- 开启动态方法调用 -->  
     <constant name="struts.enable.DynamicMethodInvocation" value="true" />
     <constant name="struts.convention.action.disableScanning" value="false" />
     <!-- 
     <constant name="struts.convention.default.parent.package" value="interceptor-package" />
     <constant name="struts.convention.package.locators.basePackage" value="com.ppstream.mt.action" /> 
 	--> 
 
<!--
 "Unable to find interceptor class referenced by ref-name XYZ". 
This means that the package where Convention is placing your actions, 
does not extend the package where the interceptor is defined 
-->

     <package name="interceptor-package" extends="convention-default">  
     
     	<interceptors>
		    <!--定义属性驱动的拦截器
       		<interceptor name="attribute" class="com.opensymphony.xwork2.interceptor.ParametersInterceptor" />-->
       		<!--定义模型驱动的拦截器
			<interceptor name="model-driven" class="com.opensymphony.xwork2.interceptor.ModelDrivenInterceptor" /> -->
			<!-- 自定义 -->
			<interceptor name="authority" class="com.ppstream.mt.interceptor.AuthorityInterceptor" >
				<param name="name">登录验证拦截器</param>  
			</interceptor>
			<interceptor name="customException" class="com.ppstream.mt.interceptor.ErrorHandlingInterceptor" >
				<param name="name">自定义异常处理拦截器</param>  
			</interceptor>
			<!-- 登录拦截器栈-->
			<interceptor-stack name="authorityStack">
				<interceptor-ref name="authority">
					<param name="excludeMethods">login</param>
				</interceptor-ref>
				<interceptor-ref name="paramsPrepareParamsStack" />
				<interceptor-ref name="defaultStack"/>
			</interceptor-stack>  
			<!-- 异常拦截器栈-->
			<interceptor-stack name="customExceptionStack">
				<!-- <interceptor-ref name="customException" />  -->
				<interceptor-ref name="paramsPrepareParamsStack" />
				<interceptor-ref name="defaultStack"/>
			</interceptor-stack>  
			
		</interceptors>
		<!-- 
		<default-interceptor-ref name="authorityStack" />   -->
	     <!-- 指定默认action
	     <default-action-ref   name = "index"   />     -->
     
	     <global-results>
	        <!-- 下面定义的结果对所有的Action都有效 -->
	        <result name="exception">/error.jsp</result>
	     </global-results>

	     <global-exception-mappings>
	        <!-- 指Action抛出Exception异常时，转入名为exception的结果。 -->
	        <exception-mapping exception="java.lang.Exception" result="exception"/>
	     </global-exception-mappings>
	    
     </package>
        
</struts>